const dice = document.getElementsByClassName('first-dice')
const colors = ['blue', 'green', 'purple', 'pink']

dice.addEventListener('click', function onClick() {
  dice.style.backgroundColor = colors[index]
})
